# UMR_Parsing_BiBL

Run: 
conda env create -f BIBL_env.yml

Activate conda env

Use bibl-inf.sh to run parsing, change the following variables:

INPUT_FILE, OUTPUT_FILE, GOLD_FILE (can be anything, isn't important), python .../BiBL/bin/predict_amrs.py, and --checkpoint
